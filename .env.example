# Copy this file to .env and fill in your values before running locally.
# On Vercel, add these as Environment Variables in the project settings.

# PostgreSQL connection string (required)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=

# Secret used to sign sessions (required — use a long random string)
SESSION_SECRET=
